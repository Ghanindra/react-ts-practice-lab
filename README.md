Typed state with useState, including union types (number | "") for controlled inputs.

Updated arrays/objects safely using functional state (prev => [...]).

Typed props for child components, including optional props and interface extensions.

Controlled inputs and proper handling of React warnings.

Converted input strings to numbers safely and validated input types.

Lifted state up by passing setters to child components.

Rendered lists with .map() and used unique key props.

Implemented toggle functionality type-safely (isAdmin).

Filtered users by search term and role, combining multiple conditions.

Persisted data in localStorage (users, filters, search) across refreshes.

Handled add vs edit with prefilled forms using useEffect.

Debugged common TypeScript + React errors.